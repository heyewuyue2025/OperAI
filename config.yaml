# OperAI 运行时配置

llm:
  temperature: 0.4
  max_tokens: 1400
  timeout_seconds: 45
  max_retries: 0
  model_d: deepseek-v4-pro
  model_c: deepseek-v4-pro

paths:
  sqlite: data/operai.sqlite3
  logs_dir: data/logs

harness:
  default_pack_id: archive
